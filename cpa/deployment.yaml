apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: cpa
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: cpa
    app.kubernetes.io/name: cpa
    app.openshift.io/runtime: java
  name: cpa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cpa
  template:
    metadata:
      labels:
        app: cpa
    spec:
      containers:
      - name: cpa
        imagePullPolicy: Always
        image: image-registry.openshift-image-registry.svc:5000/mok/cpa:40e65a853e5deddec76392fd688c1476ba1afbd0
        ports:
        - containerPort: 8381
          protocol: TCP
