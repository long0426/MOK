apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: cca
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: cca
    app.kubernetes.io/name: cca
    app.openshift.io/runtime: java
  name: cca
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cca
  template:
    metadata:
      labels:
        app: cca
    spec:
      containers:
      - name: cca
        imagePullPolicy: Always
        image: image-registry.openshift-image-registry.svc:5000/mok/cca:6a823bec7e252e7c55b862d96312413d776320e5
        ports:
        - containerPort: 8088
          protocol: TCP
